@model IEnumerable<TLCNG.entities.CAT_Bimestre1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreBimestre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdActImpPre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MesAnterior)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreBimestre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdActImpPre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MesAnterior)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdNomBim }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdNomBim }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdNomBim })
        </td>
    </tr>
}

</table>
