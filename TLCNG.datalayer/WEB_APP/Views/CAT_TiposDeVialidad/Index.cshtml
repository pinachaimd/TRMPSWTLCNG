@model IEnumerable<TLCNG.entities.CAT_TiposDeVialidad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreDeVialidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abreviatura)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreDeVialidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abreviatura)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdVialidad }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdVialidad }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdVialidad })
        </td>
    </tr>
}

</table>
